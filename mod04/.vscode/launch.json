{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/ex00/Animals", // path to the output binary
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}